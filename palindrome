#include<stdio.h>
#include<conio.h>
main()
{
	int sum=0,r=0,n,m;
	printf("Enter n value:");
	scanf("%d",&n);
	m=n;
	while(n!=0)
	{
		r=n%10;
		sum=(sum*10)+r;
		n=n/10;
	}
	if(sum==m)
	printf("It is a palindrome Number");
	else
	printf("It is not a palindrome Number");
}
