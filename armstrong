#include<stdio.h>
#include<conio.h>
main()
{
	int sum=0,r=0,n,m;
	printf("Enter n value:");
	scanf("%d",&n);
	m=n;
	while(n!=0)
	{
		r=n%10;
		sum=sum+(r*r*r);
		n=n/10;
	}
	if(sum==m)
	printf("It is a Armstrong Number");
	else
	printf("It is not a Armstrong Number");
}
