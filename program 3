#include<stdio.h>
#include<conio.h>
main()
{
	int sum=0,r=0,n;
	printf("Enter n value:");
	scanf("%d",&n);
	while(n!=0)
	{
		r=n%10;
		sum=(sum*10)+r;
		n=n/10;
    }
	printf("Reverse digits of a given number=%d",sum);
}
